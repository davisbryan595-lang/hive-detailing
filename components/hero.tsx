export function Hero() {
  return (
    <section id="home" className="sr-only" aria-hidden="true">
      {/* Deprecated: Hero is now composed directly in app/page.tsx to support the before–after slider layout */}
    </section>
  )
}
